using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TangramPlace : MonoBehaviour
{
    [SerializeField] GameObject _rightTangramPiece;

    public GameObject RightTangramPiece { get => _rightTangramPiece; set => _rightTangramPiece = value; }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
